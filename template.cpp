#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    while (t--)
    {
        int n, target;
        cin >> n >> target;
        // string s;
        // getline(cin, s);

        vector<int> arr(n);

        for (int i = 0; i < n; i++)
            cin >> arr[i];
    }
}
